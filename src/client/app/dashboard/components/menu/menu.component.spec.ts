
// export function main() {
// 	describe('Menu bar', () => {
// 		it('should open edit menu if user click edit', () => { console.log('waiting for body'); });
// 		//body still has to be written
// 		// it('should close edit menu if user click done', () => {});

// 		// it('should check dirty dashboard when user click done', () => {});

// 		// it('should save dashboard and change menu if user click yes', () => {});

// 		// it('should restore dashboard and change k;menu if user click no', () => {});

// 		// it('should not do change menu or dashboard if user click cancel', () => {});

// 		// it('should update data panel property menu when data panel options changed', () => {});
// 	});
// }
export function main() {
	console.log('test');
}
